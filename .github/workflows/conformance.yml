name: OpenVX Conformance Tests

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - uses: dtolnay/rust-toolchain@stable
      - name: Build rustVX
        run: cargo build --release
      - name: Build CTS
        run: |
          mkdir -p OpenVX-cts/build
          cd OpenVX-cts/build
          cmake ..
          make -j$(nproc)
      - name: Run Smoke Tests
        run: |
          cd OpenVX-cts/build
          export LD_LIBRARY_PATH=../../target/release
          export VX_TEST_DATA_PATH=../test_data
          timeout 60 ./bin/vx_test_conformance --filter="SmokeTest.*"

  baseline-data:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        run: cargo build --release
      - name: Build CTS
        run: |
          mkdir -p OpenVX-cts/build
          cd OpenVX-cts/build
          cmake ..
          make -j$(nproc)
      - name: Run Baseline Data Tests
        run: |
          cd OpenVX-cts/build
          export LD_LIBRARY_PATH=../../target/release
          export VX_TEST_DATA_PATH=../test_data
          timeout 120 ./bin/vx_test_conformance --filter="Scalar.*:Array.*:ObjectArray.*:GraphCallback.*:GraphDelay.*"

  vision-pixel:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        run: cargo build --release
      - name: Build CTS
        run: |
          mkdir -p OpenVX-cts/build
          cd OpenVX-cts/build
          cmake ..
          make -j$(nproc)
      - name: Run Vision Pixel Tests
        run: |
          cd OpenVX-cts/build
          export LD_LIBRARY_PATH=../../target/release
          export VX_TEST_DATA_PATH=../test_data
          timeout 300 ./bin/vx_test_conformance --filter="Scale:*:Remap:*:WarpPerspective:*:WarpAffine:*:Convolve:*:Threshold:*:LUT:*:SwapImageHandle:*"

  vision-filters:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        run: cargo build --release
      - name: Build CTS
        run: |
          mkdir -p OpenVX-cts/build
          cd OpenVX-cts/build
          cmake ..
          make -j$(nproc)
      - name: Run Vision Filter Tests
        run: |
          cd OpenVX-cts/build
          export LD_LIBRARY_PATH=../../target/release
          export VX_TEST_DATA_PATH=../test_data
          timeout 180 ./bin/vx_test_conformance --filter="Box3x3:*:Gaussian3x3:*:Median3x3:*:Dilate3x3:*:Erode3x3:*:Sobel3x3:*:Magnitude:*:Phase:*:HalfScaleGaussian:*:WeightedAverage:*:NonLinearFilter:*"

  vision-arithmetic:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        run: cargo build --release
      - name: Build CTS
        run: |
          mkdir -p OpenVX-cts/build
          cd OpenVX-cts/build
          cmake ..
          make -j$(nproc)
      - name: Run Vision Arithmetic Tests
        run: |
          cd OpenVX-cts/build
          export LD_LIBRARY_PATH=../../target/release
          export VX_TEST_DATA_PATH=../test_data
          timeout 180 ./bin/vx_test_conformance --filter="vxMultiply:*:vxAddSub:*:vxBinOp8u:*:vxuAddSub:*:vxuMultiply:*:vxuConvertDepth:*:vxuBinOp8u:*:vxNot:*"

  vision-color:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        run: cargo build --release
      - name: Build CTS
        run: |
          mkdir -p OpenVX-cts/build
          cd OpenVX-cts/build
          cmake ..
          make -j$(nproc)
      - name: Run Vision Color/Channel Tests
        run: |
          cd OpenVX-cts/build
          export LD_LIBRARY_PATH=../../target/release
          export VX_TEST_DATA_PATH=../test_data
          timeout 120 ./bin/vx_test_conformance --filter="ColorConvert:*:ChannelExtract:*:ChannelCombine:*"

  vision-features:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        run: cargo build --release
      - name: Build CTS
        run: |
          mkdir -p OpenVX-cts/build
          cd OpenVX-cts/build
          cmake ..
          make -j$(nproc)
      - name: Run Vision Feature Tests
        run: |
          cd OpenVX-cts/build
          export LD_LIBRARY_PATH=../../target/release
          export VX_TEST_DATA_PATH=../test_data
          timeout 300 ./bin/vx_test_conformance --filter="HarrisCorners:*:FastCorners:*:vxCanny:*:Histogram:*"

  graph-topology:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        run: cargo build --release
      - name: Build CTS
        run: |
          mkdir -p OpenVX-cts/build
          cd OpenVX-cts/build
          cmake ..
          make -j$(nproc)
      - name: Run Graph Topology Tests
        run: |
          cd OpenVX-cts/build
          export LD_LIBRARY_PATH=../../target/release
          export VX_TEST_DATA_PATH=../test_data
          timeout 180 ./bin/vx_test_conformance --filter="Graph.Node:*:Graph.Verify:*:Graph.Process:*:Graph.Delay:*:Graph.Age:*"